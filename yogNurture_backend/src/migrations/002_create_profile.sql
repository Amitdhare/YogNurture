 ✅ Create Profile Table (Run once in MySQL)
    CREATE TABLE profiles (
   id INT AUTO_INCREMENT PRIMARY KEY,
   name VARCHAR(100),
    age INT,
   gender VARCHAR(10),
   weight INT,
   lifestyle VARCHAR(50),
   medical_history TEXT
 );